# OceanEmbed — deployed app runtime.
#
# The dashboard's Run Pipeline action prepares data, trains, evaluates, and
# rebuilds its assets on the host. It therefore needs the complete pipeline
# environment, not merely the pre-baked-chart viewer dependencies.

-r requirements-pipeline.txt

streamlit==1.63.0     # pinned: the dashboard CSS targets this version's DOM
plotly>=5.24
pandas>=2.0
numpy>=1.26
