# OceanEmbed — dashboard runtime (Phase 4).
#
# This is the ONLY thing the Streamlit demo needs. It reads pre-baked
# artefacts (dashboard/assets.npz + outputs/metrics/*.csv / *.npz) and never
# imports torch / xarray / netCDF4 / gsw, so the deploy stays small and fast.
#
# To rebuild data, train, or evaluate, use requirements-pipeline.txt instead
# (adds xarray, netCDF4, torch, gsw, erddapy, …) — see RUN.md.

streamlit==1.63.0     # pinned: the dashboard CSS targets this version's DOM
plotly>=5.24
pandas>=2.0
numpy>=1.26
