# OceanEmbed — dependencies
#
# Python: target was 3.11 (CLAUDE.md §1); actual env is 3.14 — flagged, low risk.
#   All of the below install on 3.14. `argopy` does NOT (its aiohttp dep has no
#   3.14 wheel + no build toolchain here) — not needed; we use erddapy / raw
#   ERDDAP against AOML instead.
#
# GPU: this PC has no CUDA (Intel Iris Xe) so torch here is the CPU build.
#   On the Lenovo LOQ install the CUDA build instead — see RUN.md.

# --- data access + pipeline ---
numpy
pandas
scipy
xarray
netCDF4
h5netcdf
h5py
pyarrow
requests
erddapy
pydap
python-dateutil

# --- modelling ---
torch                 # CPU build here; `--index-url .../cu124` on the LOQ (RUN.md)
torchvision
gsw                   # TEOS-10 — regime climatology + physics diagnostics

# --- evaluation + viz ---
matplotlib

# --- dashboard (Phase 4) ---
streamlit
plotly

# --- optional: Prithvi-EO geospatial backbone (§5.1) ---
# terratorch          # pip install separately on the LOQ; backbone.py falls back to resnet50

# --- optional / not used ---
# xesmf               # conda-only on Windows; nearest-neighbour regrid in xarray suffices
# huggingface_hub     # pulled in by terratorch when Prithvi is used

# --- dev ---
pytest
